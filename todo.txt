terraform
    spin up test account to test things
    certificate signing thing route 53
    better least permissions on ecs instance, task execution, codebuild service roles
    auto delete route53 record
    filter sns topic to upsert lambda if possible
    move codebuild logs to cloudfront?
gui web
    decom and replace with mcplugin
gui py
    consider replacing with mcmod
    build for distribution
    optionally read credentials from file so aws cli isn't needed
    xero mans confusing bash script thing to run the server
minecraft
    kill empty server task via plugin or datapack
refactor
    remove hardcoded values
    handle errors
    clean up code?
akl region
    switch to IPv6
    upgrade to Amazon Linux 2023 (or whatever the next one will be)
    more
